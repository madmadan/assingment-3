#include <iostream>
using namespace std;

int main() {

              int num;
              cout<<"enter year";
              cin>>num;
              
              if(num%4 == 0.0)
              {
                 cout<<"year is leap year";
              }
              else
              {
                 cout<<"year is not leap year";
              }
            
	return 0;
}
